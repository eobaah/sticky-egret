import React, { Component } from 'react'
import { render } from 'react-dom'
import { Modal, Button } from 'elemental'

import App from './App.js'

render( <App />, document.getElementById('mountMeNow'))
